# Steakhouse Demo Assets

This folder is the asset home for the default NOVA STEAKHOUSE template.

For a new restaurant, replace or extend assets here and update:

- `src/config/restaurant.ts`
- `src/data/menu.ts`

Keep brand, hero, menu, and icon assets grouped by restaurant/theme instead of hardcoding scattered paths in components.
